{
  "name": "bunyan-app",
  "version": "1.0.0",
  "description": "تطبيق بنيان لمصروفات البناء",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "build-android": "npx @capacitor/cli add android && npx @capacitor/cli copy android && npx @capacitor/cli open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
